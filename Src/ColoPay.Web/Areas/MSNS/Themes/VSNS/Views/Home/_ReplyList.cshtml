@using Webdiyer.WebControls.Mvc
@model YSWL.MALL.ViewModel.SNS.GroupInfo
            @foreach (var item in Model.TopicList)
            {
                Html.RenderAction("TopicReply", "Home", new { id = item.TopicID });

            }
      
